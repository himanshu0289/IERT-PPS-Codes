#include<stdio.h>
#include<conio.h>
int main(){
    float fn,n;
    printf("Enter floating Num : ");
    scanf("%f",&fn);
    n = (int)fn;
    printf("Intger equivalent to %f : %d",fn,n);
    return 0;
}